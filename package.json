{
  "name": "playwright",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "allureGenerate":"allure generate allure-results --clean -o allure-report",
    "allureOpen":"allure open allure-report",
    "test" : "npx playwright test",
    "headed test" : "npx playwright test --headed",
    "debug" : "npx playwright test --debug",
    "uimode" : "npx playwright test --ui",
    "config" : "npx playwright test --config=playwright.config_1.js",
    "chrome" : "npx playwright test --config=playwright.config_1.js --project=ChromeProject",
    "fireFox" : "npx playwright test --config=playwright.config_1.js --project=FireFoxProject",
    "webKit" : "npx playwright test --config=playwright.config_1.js --project=WebkitProject",
    "webtag" : "npx playwright test --grep @web",
    "codegen" : "npx playwright codegen"

  },

  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^26.1.2",
    "allure-playwright": "^3.12.0"
  },
  "dependencies": {
    "exceljs": "^3.4.0"
  }
}
